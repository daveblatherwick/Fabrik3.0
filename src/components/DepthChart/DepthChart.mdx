import { Meta, Title, Primary, Controls, Stories } from "@storybook/addon-docs/blocks";
import { ComponentMeta } from "../../docs/ComponentMeta";
import * as DepthChartStories from "./DepthChart.stories";

<Meta of={DepthChartStories} />

<Title />

A vertical depth chart paired with the OrderBook. Visualises cumulative ask and bid volume as two mirrored area curves around the spread.

<ComponentMeta
  status="alpha"
  version="0.1.0"
  owner="Dave Blatherwick"
  lastUpdated="2026-04-22"
  figma={{
    url: "https://www.figma.com/design/3IvLTqWEAlVjzcieRQd8vc/Crypto-PE-Screens?node-id=542-29365",
    label: "Depth Chart (Crypto PE Screens)",
    nodeId: "542:29365",
  }}
  designNotes={[
    "Single inline SVG with two area paths — no chart library, no runtime dependency.",
    "Ask area uses `--fabric-depthchart-ask-*` tokens; bid area uses `--fabric-depthchart-bid-*` which resolve to the current brand dark shade (Adaptive purple / Aeron green / Demo blue).",
    "Three horizontal gridlines act as volume reference markers; labels are fed via the `labels` prop so they can show domain-meaningful values.",
    "Width is fixed at 184px to match the Figma sidebar slot; height is flexible via the `height` prop.",
  ]}
  a11yNotes={[
    "SVG has role=img and aria-label so assistive tech announces it as a chart.",
    "Gridline labels are real <text> elements inside the SVG, not images — they're readable by screen readers and indexable.",
    "Icon buttons in the header have aria-label for sort direction and export.",
  ]}
  limitations={[
    "No hover crosshair or tooltip yet (Figma shows markers at gridlines but no interaction spec).",
    "Area paths are linear — no spline smoothing between levels.",
    "Labels are static props; they don't auto-compute from the data when using static mode.",
    "Chart is always split 50/50 between asks and bids regardless of actual spread asymmetry.",
  ]}
  changelog={[
    {
      date: "2026-04-22",
      version: "0.1.0",
      changes: [
        "Initial port from Crypto PE Screens Figma frame.",
        "Themed via tokens: ask/bid fills, grid, labels all resolve per light/dark.",
        "Live story wired to Binance BTCUSDT depth, 20 levels, 2s polling.",
      ],
    },
  ]}
/>

<Primary />

<Controls />

<Stories />
