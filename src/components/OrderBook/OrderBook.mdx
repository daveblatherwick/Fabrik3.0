import { Meta, Title, Primary, Controls, Stories } from "@storybook/addon-docs/blocks";
import { ComponentMeta } from "../../docs/ComponentMeta";
import * as OrderBookStories from "./OrderBook.stories";

<Meta of={OrderBookStories} />

<Title />

A real-time order book for a crypto exchange. Shows asks, bids, spread, and cumulative depth. Designed for the narrow sidebar of a trading workspace.

<ComponentMeta
  status="alpha"
  version="0.2.0"
  owner="Dave Blatherwick"
  lastUpdated="2026-04-22"
  figma={{
    url: "https://www.figma.com/design/3IvLTqWEAlVjzcieRQd8vc/Crypto-PE-Screens?node-id=542-29316",
    label: "Order Book (Crypto PE Screens)",
    nodeId: "542:29316",
  }}
  designNotes={[
    "3-column grid (Price / Amount / Total) driven by CSS grid `1fr 1fr 1fr`; fits the 300px sidebar without hardcoded column widths.",
    "Depth bar is a right-anchored absolutely-positioned element whose width is derived from `row.total / max(total)`. No JS layout math.",
    "Asks and bids are coloured via component tokens (--fabric-orderbook-ask / -bid) that resolve per theme: darker reds/greens on light, bright on dark.",
    "Spread row always renders in the centre, between asks and bids, with last-matched price + absolute and percent spread.",
  ]}
  a11yNotes={[
    "Uses role=table / role=row / role=columnheader for assistive navigation of the book.",
    "Icon buttons in the header expose aria-label (Sort direction / Export).",
    "Numeric columns use font-variant-numeric: tabular-nums so digits align visually for scanning.",
  ]}
  limitations={[
    "No live-update animation or flash on price change.",
    "Depth bars scale against a single max across both sides; a real book might want per-side normalisation.",
    "No grouping/aggregation control (e.g. 0.1 / 1 / 10 USD bucket sizes).",
    "No keyboard navigation of rows; not wired to any data source.",
  ]}
  changelog={[
    {
      date: "2026-04-22",
      version: "0.2.0",
      changes: [
        "Tokenised: ask/bid, backgrounds, dividers, depth bar fills now all read from --fabric-* variables.",
        "Light and dark mode render correctly; density (compact/default/spacious) scales row heights and font sizes.",
        "Column widths switched to CSS grid fr units (was 97/85/102 hardcoded).",
      ],
    },
    {
      date: "2026-04-22",
      version: "0.1.0",
      changes: [
        "Initial port from Crypto PE Screens Figma frame.",
        "3 columns, ~20 rows, spread row, depth bars.",
      ],
    },
  ]}
/>

<Primary />

<Controls />

<Stories />
