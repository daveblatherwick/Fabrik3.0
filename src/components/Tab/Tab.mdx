import { Meta, Title, Primary, Controls, Stories } from "@storybook/addon-docs/blocks";
import { ComponentMeta } from "../../docs/ComponentMeta";
import * as TabStories from "./Tab.stories";

<Meta of={TabStories} />

<Title />

A horizontal list of sibling views. Ships as a composable `Tabs` container plus an individual `Tab` primitive. Supports controlled / uncontrolled modes, disabled tabs, and an optional badge per tab.

<Primary />

<Controls />

<Stories />

<ComponentMeta
  status="alpha"
  version="0.1.0"
  owner="Dave Blatherwick"
  lastUpdated="2026-04-22"
  figma={{
    url: "https://www.figma.com/design/3AZXedsXs7QlKc5dJmzQfl/Adaptive---Design-System?node-id=4197-6434",
    label: "Tab (Fabric DS)",
    nodeId: "4197:6434",
  }}
  designNotes={[
    "Indicator is a 2px bottom border on the active tab, pulled under the list's divider via margin-bottom: -1px.",
    "Tabs accepts either internal state (defaultValue) or controlled state (value + onValueChange) — same pattern as Radix primitives.",
    "Per-tab badge uses a minimal inline pill; switches to brand-50 / brand-700 colouring on the active tab.",
  ]}
  a11yNotes={[
    "role=tablist on the container, role=tab with aria-selected on each button — standard ARIA tabs pattern.",
    "Focus ring uses --fabric-focus-ring with a small rounded corner so it clears the bottom indicator line.",
    "Disabled tabs set disabled on the button element; not skipped by keyboard navigation yet.",
  ]}
  limitations={[
    "No arrow-key navigation between tabs (keyboard moves focus but tabs don't auto-activate).",
    "No vertical / tab-direction variant.",
    "No panel/content component — consumer renders panels themselves based on the value.",
  ]}
  changelog={[
    {
      date: "2026-04-22",
      version: "0.1.0",
      changes: [
        "Initial variant: Tabs with items array, optional disabled, optional badge.",
        "Active underline + brand-coloured label via tokens.",
      ],
    },
  ]}
/>
