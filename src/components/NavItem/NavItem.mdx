import { Meta, Title, Primary, Controls, Stories } from "@storybook/addon-docs/blocks";
import { ComponentMeta } from "../../docs/ComponentMeta";
import * as NavItemStories from "./NavItem.stories";

<Meta of={NavItemStories} />

<Title />

A single row in a side or top navigation. Supports a leading icon, trailing badge/shortcut, and an active state. Renders as `<button>` or `<a>`.

<Primary />

<Controls />

<Stories />

<ComponentMeta
  status="alpha"
  version="0.1.0"
  owner="Dave Blatherwick"
  lastUpdated="2026-04-22"
  figma={{
    url: "https://www.figma.com/design/3AZXedsXs7QlKc5dJmzQfl/Adaptive---Design-System?node-id=7030-1565",
    label: "Navigation Item (Fabric DS, 14 variants)",
    nodeId: "7030:1565",
  }}
  designNotes={[
    "Polymorphic via the `as` prop — defaults to <button>, switches to <a> when a href/to is needed. Types are split into two unions so each only accepts its valid attributes.",
    "Active state uses brand-50 (light) / translucent brand (dark) background with brand text and icon colour.",
    "Icon colour follows currentColor so active rows tint the icon automatically.",
  ]}
  a11yNotes={[
    "aria-current='page' applied when `active` is true — standard pattern for navigation.",
    "Focus ring rendered inside the row (outline-offset: -2px) so it doesn't clip against tight container edges.",
    "Labels truncate with ellipsis rather than wrapping; use tooltips at the call-site if truncation is common.",
  ]}
  limitations={[
    "No collapsed/icon-only mode for collapsed sidebars.",
    "No submenu / nesting.",
    "No external-link visual indicator.",
  ]}
  changelog={[
    {
      date: "2026-04-22",
      version: "0.1.0",
      changes: [
        "Initial variant: icon + label + optional trailing (badge or shortcut).",
        "Polymorphic rendering as button or anchor.",
        "Themed active/hover/disabled via tokens.",
      ],
    },
  ]}
/>
