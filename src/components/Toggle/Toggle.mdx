import { Meta, Title, Primary, Controls, Stories } from "@storybook/addon-docs/blocks";
import { ComponentMeta } from "../../docs/ComponentMeta";
import * as ToggleStories from "./Toggle.stories";

<Meta of={ToggleStories} />

<Title />

A switch for boolean on/off state. Three sizes, optional label, supports controlled and uncontrolled usage.

<Primary />

<Controls />

<Stories />

<ComponentMeta
  status="alpha"
  version="0.1.0"
  owner="Dave Blatherwick"
  lastUpdated="2026-04-22"
  figma={{
    url: "https://www.figma.com/design/3AZXedsXs7QlKc5dJmzQfl/Adaptive---Design-System?node-id=6047-7971",
    label: "Toggle (Fabric DS, 32 variants)",
    nodeId: "6047:7971",
  }}
  designNotes={[
    "Native <input type='checkbox'> visually hidden; a styled track + thumb span use the :checked pseudo-class via ~ combinator for the on-state colour.",
    "Track + thumb sizes are CSS variables on the track so the three sizes (sm/md/lg) just override those vars — no duplicated rules.",
    "Thumb animates its left offset with a 140ms ease; runs on the compositor, no layout thrash.",
  ]}
  a11yNotes={[
    "Native checkbox semantics — assistive tech announces it as a switch/toggle, keyboard works by default.",
    "Focus ring applies to the track when the underlying input is :focus-visible.",
    "Click target is the whole label (track + label text).",
  ]}
  limitations={[
    "Uses checkbox role rather than the ARIA switch role; assistive announcements say 'checkbox' not 'switch'.",
    "No loading / pending state for async toggles.",
    "No icon-in-track variant.",
  ]}
  changelog={[
    {
      date: "2026-04-22",
      version: "0.1.0",
      changes: [
        "Initial variant: sm / md / lg sizes, on / off / disabled.",
        "Brand-coloured on-state via --fabric-brand-600.",
      ],
    },
  ]}
/>
